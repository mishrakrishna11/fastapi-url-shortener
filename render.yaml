services:
  - type: web
    name: fastapi-url-shortener
    env: python
    plan: free
    buildCommand: ""
    startCommand: ./start.sh
    envVars:
      - key: DATABASE_URL
        value: YOUR_DATABASE_URL
